# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17
# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=1.21.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.21.1]
# The Neo version must agree with the Minecraft version to get a valid artifact
neo_version=21.1.248
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=apoli
# The human-readable display name for the mod.
mod_name=Apoli
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=The A/O License
# The mod version. See https://semver.org/
mod_version=1.97.0
# Maven coordinate version — loader+MC-qualified so all Apoli variants coexist in mavenLocal.
# The in-game mod version (neoforge.mods.toml) stays mod_version; this only affects the published GAV.
maven_version=1.97.0+1.21.1-neoforge
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=dev.overgrown.apoli

# Icarus (compile-only, optional at runtime) — the Modrinth version_number for NeoForge 1.21.1.
# Bump per https://modrinth.com/mod/icarus/versions.
icarus_version=4.7.6+NeoForge
# Figura (compile-only, optional at runtime). Figura reuses the same version_number across
# loaders, so the Modrinth maven needs the VERSION ID, not the number.
# AvgeQoZm = Figura 0.1.6+1.21.1 (NeoForge). Bump per https://modrinth.com/mod/figura/versions.
figura_version=AvgeQoZm
# Accessory-slot mods (compile-only, optional at runtime; gated by ModCompat / Accessories facade).
accessories_version=1.1.0-beta.53+1.21.1
curios_version=9.5.1+1.21.1
# Hardcore Revival + Balm (compile-only, optional at runtime; gated by ModCompat.HARDCORE_REVIVAL).
balm_version=21.0.60+1.21.1
hardcore_revival_version=21.1.9+1.21.1
# Simple Voice Chat API (compile-only, optional at runtime) — the voice_* powers/conditions.
voicechat_api_version=2.6.0
# Vosk offline speech (bundled via jarJar) — the browser-less speech fallback.
vosk_version=0.3.45

skinlayers_version=xPYbAPfz

ears_version=Dxc2NR3x
puffish_skills_version=PFl7yr35

# KubeJS + Rhino (compile-only, optional at runtime) — the apoli:script types and ApoliEvents.
kubejs_version=2101.7.2-build.372
rhino_version=2101.2.8-build.91

sodium_version=mc1.21.1-0.6.13-neoforge

yigd_version=Axk4bfXT
gravestone_version=rMGJpjaM
